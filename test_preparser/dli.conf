# GnuCOBOL external preparser configuration for IMS/DLI (mock)
#
# This simulates an IBM IMS/DL/I preprocessor registration.
# In the mainframe pipeline: SQL → DLI → CICS → cobc
# (order depends on which EXEC block appears first in source).

subsystem:      DLI
command:        /home/utam1/gnucobol/test_preparser/mock_dli_preparse.sh
on-error:       warn
